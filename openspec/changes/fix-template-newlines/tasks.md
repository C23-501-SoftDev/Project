## 1. Template Resources

- [x] 1.1 Вынести Markdown-контент всех системных шаблонов в отдельные UTF-8 `.md` файлы.
- [x] 1.2 Сохранить `005-insert-system-templates.xml` неизменённым для совместимости checksum уже применённых миграций; исправление выполняется последующим changeset.

## 2. Existing Data Migration

- [x] 2.1 Добавить новый Liquibase changeset после текущих миграций для обновления `templates.content` у известных системных шаблонов из `.md` файлов.
- [x] 2.2 Ограничить update системными шаблонами (`is_system = true`) и конкретными именами шаблонов.

## 3. Backend Behavior

- [x] 3.1 Проверить `POST /api/documents`: если response возвращает устаревший `request.content` вместо фактического template content, вернуть фактически сохранённый content.
- [x] 3.2 Убедиться, что `DocumentService.createDocument` продолжает применять автонумерацию требований после загрузки корректного Markdown шаблона.

## 4. Tests

- [x] 4.1 Добавить тест, что системный шаблон `Описание архитектуры` содержит реальные переносы строк и строку `## Обзор системы` как отдельный Markdown heading.
- [x] 4.2 Добавить тест создания документа из шаблона, проверяющий, что сохранённый Git content не схлопнут в одну строку.
- [x] 4.3 Добавить или обновить тест, что шаблон `Требования к системе` после исправления Markdown всё ещё нумерует требования в формате `REQ-001`.

## 5. Quality Gates

- [x] 5.1 Выполнить релевантные backend-тесты.
- [ ] 5.2 Проверка критериев качества согласно [openspec/quality-gates.md](../../quality-gates.md).
