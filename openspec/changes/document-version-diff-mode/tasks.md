## 1. Domain and persistence

- [x] 1.1 Добавить доменные модели `DocumentDiff`, `DiffLine` и тип строки diff.
- [x] 1.2 Расширить `DocumentVersionRepository` поиском версии по паре `documentId` и `gitHash`.
- [x] 1.3 Реализовать Spring Data/JPA query и adapter чтения версии без изменения существующих записей `versions`.

## 2. Git diff backend

- [x] 2.1 Расширить Git content port операцией сравнения одного документного пути в двух commit SHA.
- [x] 2.2 Реализовать JGit adapter: чтение blob выбранного пути из обоих commit tree и `DiffFormatter` для `from` → `to`.
- [x] 2.3 Преобразовать результат в typed строки с номерами до/после, исключив sidecar-метаданные и другие файлы коммита.
- [x] 2.4 Валидировать SHA, обрабатывать отсутствующий blob как пустое состояние и ограничить максимальное число строк результата.

## 3. Application and REST API

- [x] 3.1 Реализовать use case/service, который подтверждает принадлежность обоих SHA документу до обращения к Git.
- [x] 3.2 Добавить `GET /api/documents/{id}/diff?from={hash1}&to={hash2}`, DTO и OpenAPI-описание.
- [x] 3.3 Настроить `READ`-авторизацию и согласованные ответы `400`, `403`, `404`, `422` и `5xx`.
- [x] 3.4 Обновить `BACKTRACKER.md` — актуализировать новый endpoint, его статус и дату в шапке.

## 4. Diff view

- [x] 4.1 Обновить `pages/document-history.html`: выбор двух версий, действие «Сравнить» и состояния загрузки/ошибки/пустого diff.
- [x] 4.2 Добавить JS-модуль запроса diff и безопасного рендеринга через DOM text nodes.
- [x] 4.3 Добавить CSS GitHub-style для удалённых, добавленных и контекстных строк с номерами.
- [x] 4.4 Связать выбор с endpoint истории версий после его реализации, сохранив возможность передавать две SHA в diff-модуль напрямую.

## 5. Tests and verification

- [x] 5.1 Unit: сервис отклоняет одинаковые/некорректные SHA и SHA чужого документа до вызова Git port.
- [x] 5.2 Integration: JGit временный repository возвращает добавленные, удалённые и контекстные строки только для файла документа.
- [x] 5.3 MVC/integration: проверить `200`, `400`, `403`, `404`, `422` и отсутствие утечки содержимого чужой версии.
- [x] 5.4 Frontend: проверить безопасный рендеринг HTML-подобного текста и визуальные классы added/removed/context.
- [x] 5.5 Проверка критериев качества согласно `openspec/quality-gates.md`: выполнить `mvn clean compile`, `mvn test` и при доступной PostgreSQL smoke-check `/actuator/health`.

## 6. Text-aware comparison algorithms

- [x] 6.1 Добавить типы алгоритма и сегмента diff, расширить доменную и REST-модель без нарушения проверки доступа и лимитов.
- [x] 6.2 Реализовать посимвольный, пословный, гибридный и существующий построчный алгоритмы; сделать `HYBRID` значением по умолчанию.
- [x] 6.3 Расширить endpoint параметром `algorithm`, валидацией значений и OpenAPI-описанием; обновить `BACKTRACKER.md`.
- [x] 6.4 Добавить выбор алгоритма в history UI и безопасную отрисовку сегментов в однопанельном и двухпанельном режимах.
- [x] 6.5 Добавить unit, integration и JavaScript-тесты для кириллицы, пунктуации, внутрисловных правок, отображения сегментов и всех алгоритмов.
- [ ] 6.6 Проверка критериев качества согласно `openspec/quality-gates.md`: выполнить `mvn clean compile`, `mvn test`, JavaScript-тесты и при доступной PostgreSQL smoke-check `/actuator/health`.
- [x] 6.7 Убрать дублирование заменённых строк: возвращать одну `MODIFIED`-запись, а для `CHARACTER` и `WORD` сравнивать цельный текстовый поток.
- [x] 6.8 Улучшить usability страницы истории: оставить в UI символьный и построчный режимы, добавить фиолетовое основное действие, спокойную подсветку и ссылку «История и сравнение» на странице документа.
- [x] 6.9 Сделать селекты версий доступными до ответа API и привести кнопки переключения представления и контекста к стилю страницы diff.
