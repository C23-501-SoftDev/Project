## Context

В системе базы знаний уже реализована базовая модель документов (`Document`) с метаданными в PostgreSQL и содержимым в Git. Текущая задача — внедрить полноценный жизненный цикл документов со статусами `Draft` (Черновик) и `Published` (Опубликован), обеспечив строгий контроль доступа и правила публикации.

## Goals / Non-Goals

**Goals:**
- Закрепить статусы `Draft` и `Published` в бизнес-логике и модели данных.
- Обеспечить автоматическое присвоение статуса `Draft` при создании нового документа.
- Реализовать фильтрацию на уровне репозитория и сервиса: чужие черновики не должны быть видны при получении по ID, в списках, поиске и пагинации.
- Реализовать операцию публикации документа с переводом статуса из `Draft` в `Published` и валидацией прав (публиковать может только автор или администратор).
- Обеспечить доступность опубликованных (`Published`) документов для чтения всем пользователям, имеющим доступ к соответствующему пространству.
- Сохранить обратную совместимость с существующей логикой статуса `Deleted` (soft delete).
- Проверить существование поля `status` в схеме БД и избежать лишних миграций при его наличии.

**Non-Goals:**
- Реализация сложных рабочих процессов согласования (workflow с дополнительными статусами вроде `Review` или `Archived`).
- Изменение механизма хранения контента в Git.

## Decisions

### 1. Модель данных и миграции (Database)
- Проверить текущую сущность `Document` и схему БД на наличие поля `status`.
- Если поле отсутствует, добавить Liquibase миграцию для добавления колонки `status` (по умолчанию `Draft`).
- Значения статусов: `Draft`, `Published`, а также сохранение поддержки `Deleted`.

### 2. Бизнес-логика и безопасность (Application & Domain Layers)
- **Создание документа**: При создании нового документа (`POST /api/documents`) поле `status` явно устанавливается в `Draft`, а текущий авторизованный пользователь фиксируется как автор (`authorId`).
- **Публикация документа**: Добавить метод/эндпоинт публикации (например, `POST /api/documents/{id}/publish` или обновление через `PUT`). Бизнес-логика проверяет:
  1. Документ существует и не удален (`status != Deleted`).
  2. Текущий пользователь является автором документа (`authorId == currentUserId`) либо обладает ролью администратора.
  3. Документ находится в статусе `Draft`.
- **Видимость и фильтрация**:
  - Получение по ID (`GET /api/documents/{id}`): Если документ в статусе `Draft`, он возвращается только если текущий пользователь является автором или администратором. В противном случае возвращается `404 Not Found` или `403 Forbidden` (согласно принятой практике безопасности в проекте).
  - Списки, поиск и пагинация: Запросы на получение списка документов в пространстве или общие выборки автоматически фильтруются: пользователи видят опубликованные документы (`Published`) своего пространства, а свои собственные черновики (`Draft`) видят только авторы и администраторы.

### 3. REST API (Interfaces Layer)
- Добавление эндпоинта для публикации: `POST /api/documents/{id}/publish` (или аналогичный в `DocumentController`).
- Обновление существующих эндпоинтов выборки с учетом правил видимости.

## Risks / Trade-offs

- **[Risk]** Усложнение SQL-запросов фильтрации (комбинация прав пространства, ролей администратора, авторства и статусов).
  - **Mitigation** Выделение логики проверки прав и построения спецификаций / критериев фильтрации в сервисный слой (или JPA Specification).

## Proposed Code Changes

- **Domain / Service**: `DocumentService.java` — добавление метода публикации с валидацией прав, доработка методов поиска/получения с учетом статусов.
- **Repository**: `JpaDocumentRepository.java` — расширение методов выборки с учетом статусов `Draft`, `Published` и прав пользователя.
- **Controller**: `DocumentController.java` — новый эндпоинт публикации документа.
- **Database**: Проверка схемы БД / Liquibase миграция (при необходимости добавления поля `status`).