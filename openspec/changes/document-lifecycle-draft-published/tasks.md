## 1. Database & Model Verification

- [x] 1.1 Проверить существующую схему базы данных и сущность `Document` на наличие поля `status`
- [x] 1.2 При отсутствии поля `status` в таблице документов подготовить Liquibase миграцию (поле уже существовало и поддерживалось в БД/entity)
- [x] 1.3 Обновить JPA сущность `Document` (поддержка статусов `Draft`, `Published`, сохранение `Deleted`)

## 2. Domain & Application Layer

- [x] 2.1 Добавить правила валидации и исключения для проверки прав при публикации (например, `DocumentAccessDeniedException` или использование существующих механизмов)
- [x] 2.2 Реализовать проверку прав на публикацию в `DocumentService` (публиковать может только автор документа или администратор)
- [x] 2.3 Обновить логику создания документа: новый документ автоматически создается со статусом `Draft` и текущим пользователем в качестве автора
- [x] 2.4 Обновить логику методов получения (`findById`), списков, поиска и пагинации с учетом видимости:
    - Чужие черновики (`Draft`) не отображаются для обычных пользователей
    - Черновики видны только их автору и администраторам
    - Опубликованные (`Published`) документы видны всем пользователям с доступом к пространству
    - Существующая логика `Deleted` остается неизменной

## 3. Interfaces Layer (REST API)
- [x] 3.1 Добавить эндпоинт публикации документа (например, `POST /api/documents/{id}/publish`)
- [x] 3.2 Настроить ограничения доступа в Security / Spring Security для нового эндпоинта (автор или администратор)

## 4. Verification & Testing
- [x] 4.1 Написать юнит- и интеграционные тесты для проверки сценария создания документа (статус `Draft`, автор, ограничения видимости) (пропущено согласно инструкциям)
- [x] 4.2 Написать юнит- и интеграционные тесты для проверки сценария публикации (пропущено согласно инструкциям)
- [x] 4.3 Проверить, что `openspec status --change document-lifecycle-draft-published` показывает все артефакты готовыми
