## 1. Database & Model Verification

- [ ] 1.1 Проверить существующую схему базы данных и сущность `Document` на наличие поля `status`
- [ ] 1.2 При отсутствии поля `status` в таблице документов подготовить Liquibase миграцию
- [ ] 1.3 Обновить JPA сущность `Document` (поддержка статусов `Draft`, `Published`, сохранение `Deleted`)

## 2. Domain & Application Layer

- [ ] 2.1 Добавить правила валидации и исключения для проверки прав при публикации (например, `DocumentAccessDeniedException` или использование существующих механизмов)
- [ ] 2.2 Реализовать проверку прав на публикацию в `DocumentService` (публиковать может только автор документа или администратор)
- [ ] 2.3 Обновить логику создания документа: новый документ автоматически создается со статусом `Draft` и текущим пользователем в качестве автора
- [ ] 2.4 Обновить логику методов получения (`findById`), списков, поиска и пагинации с учетом видимости:
    - Чужие черновики (`Draft`) не отображаются для обычных пользователей
    - Черновики видны только их автору и администраторам
    - Опубликованные (`Published`) документы видны всем пользователям с доступом к пространству
    - Существующая логика `Deleted` остается неизменной

## 3. Interfaces Layer (REST API)
- [ ] 3.1 Добавить эндпоинт публикации документа (например, `POST /api/documents/{id}/publish`)
- [ ] 3.2 Настроить ограничения доступа в Security / Spring Security для нового эндпоинта (автор или администратор)

## 4. Verification & Testing
- [ ] 4.1 Написать юнит- и интеграционные тесты для проверки сценария создания документа (статус `Draft`, автор, ограничения видимости)
- [ ] 4.2 Написать юнит- и интеграционные тесты для проверки сценария публикации (изменение статуса на `Published`, проверка прав автора/администратора и запрет для других пользователей)
- [ ] 4.3 Проверить, что `openspec status --change document-lifecycle-draft-published` показывает все артефакты готовыми