## Задачи (Tasks)

- [ ] **1. Подготовка модели данных и миграции базы данных**
  - [ ] 1.1 Добавить поля `previous_status` (VARCHAR) и `previous_parent_id` (BIGINT) в сущность `Document` и таблицу `documents`.
  - [ ] 1.2 Создать миграцию БД для добавления новых колонок.
  - [ ] 1.3 Обновить маппинги JPA и репозиторий `DocumentRepository` для поддержки выборки удаленных (`DELETED`) документов и фильтрации.

- [ ] **2. Реализация логики Soft-delete и управления иерархией (Backend)**
  - [ ] 2.1 Обновить метод удаления (`delete`) в `DocumentService`: сохранение текущего статуса в `previous_status`, родителя в `previous_parent_id`, перевод статуса в `DELETED`.
  - [ ] 2.2 Реализовать автоматическое переподчинение прямых дочерних элементов удаляемого узла родителю верхнего уровня ("дедушке") с сохранением `previous_parent_id`.
  - [ ] 2.3 Обновить методы выборки документов (списки, поиск, пагинация, дерево), исключив из них документы со статусом `DELETED`.
  - [ ] 2.4 Защитить метод обновления (`PUT /api/documents/{id}`) от модификации документов со статусом `DELETED`.

- [ ] **3. Реализация административной корзины и восстановления (Backend)**
  - [ ] 3.1 Реализовать эндпоинт получения списка удаленных документов для корзины (`GET /api/admin/documents/recycle-bin`) с проверкой прав администратора (`is_admin = true`).
  - [ ] 3.2 Реализовать метод и эндпоинт восстановления документа (`POST /api/admin/documents/{id}/restore`):
        - Возврат предыдущего статуса из `previous_status` (`DRAFT` или `PUBLISHED`).
        - Восстановление родительской связи с проверкой доступности родителя (определение "дедушки", если прежний родитель удален).
        - Блокировка восстановления при отсутствии доступного пространства/родителя.
        - Очистка `previous_status` и `previous_parent_id`.
  - [ ] 3.3 Добавить логирование аудита (Audit log) для операций мягкого удаления и восстановления документов.

- [ ] **4. Разработка пользовательского интерфейса (Frontend)**
  - [ ] 4.1 Создать интерфейс административной корзины (`admin-recycle-bin.html` / компонент в панели администратора) для отображения списка `DELETED` документов.
  - [ ] 4.2 Добавить кнопку и обработчик восстановления документа в интерфейсе корзины.
  - [ ] 4.3 Убедиться, что обычные страницы списков, поиска и дерева документов корректно скрывают `DELETED` документы и блокируют их редактирование.